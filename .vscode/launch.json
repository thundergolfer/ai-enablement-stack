{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Generate",
            "skipFiles": [
                "<node_internals>/**"
            ],
            "program": "${workspaceFolder}/generate-image.js",
            "runtimeExecutable": "yarn",
            "runtimeArgs": ["run", "generate"]
        }
    ]
}